# Добавить в /etc/pve/lxc/105.conf (blueprint-ai) ПОСЛЕ reclaim-gpu.sh:
lxc.cgroup2.devices.allow: c 226:* rwm
lxc.mount.entry: /dev/dri dev/dri none bind,optional,create=dir
# Потом: pct reboot 105
# Внутри CT 105: apt install -y libvulkan1 mesa-vulkan-drivers && vulkaninfo --summary
